# OST Top Builder — генератор видео

Веб-приложение — генератор шаблонных видео. Три режима:

1. **Топ через турнир** — попарные сравнения треков (аудио и/или видео)
   строят рейтинг, из него рендерится видео-«топ».
2. **Ручной топ** — тот же формат, но позиции собираются вручную из пула
   медиа (аудио или видео со звуком), без турнира.
3. **Пикер** («выбери персонажа / OST / опенинг») — раунды с правилом сверху
   и 2–9 блоками: блоки появляются по одному с анимацией (видео-блок играет
   свой звук, фоновая музыка приглушается), в конце все раскрываются под
   интерактивный таймер с кольцом, опционально подсвечивается «ответ».

Доступ разграничен ролями: гость видит только приветственный экран,
пользователь работает в лимитах, админ может всё; личный кабинет управляет
файлами на сервере.

Спецификации и план — в [`.ai/`](.ai/). Видение: [`.ai/specs/00-vision.md`](.ai/specs/00-vision.md).

## Стек

- **Next.js 14** (App Router, React 18) — фронт + API.
- **Prisma + SQLite** (MVP; продакшн-таргет Postgres — смена `provider` в схеме).
- **Auth.js (next-auth v5)** — email + пароль.
- **Remotion** — рендер видео (16:9, 1080p).
- **ffmpeg-static** — декод/нарезка аудио, RMS-анализ активного сниппета.
- **Vitest** — тесты.

## Требования

Перед запуском должны быть установлены:

| Что                         | Зачем                                   | Как проверить            |
|-----------------------------|------------------------------------------|--------------------------|
| **Node.js ≥ 18.17** (лучше 20 LTS) | Next.js 14 и весь бэкенд          | `node -v`                |
| **npm** (идёт с Node)       | установка зависимостей                   | `npm -v`                 |
| **Google Chrome / Chromium** | только для **рендера MP4** (Remotion)   | `google-chrome --version` |

Ставить отдельно **не нужно**: ffmpeg (идёт в комплекте — пакет
`ffmpeg-static`), SQLite (встроен в Prisma), Remotion/React/Prisma и прочее —
всё приходит через `npm install`.

## Быстрый старт

```bash
npm install
cp .env.example .env          # отредактируйте AUTH_SECRET и ADMIN_EMAILS
npx prisma generate
npx prisma db push            # создаёт SQLite-схему (prisma/dev.db)
npm run dev                   # http://localhost:3000
```

`ADMIN_EMAILS` — email-ы (через запятую), которые автоматически получают роль
администратора при входе/регистрации.

### Браузер для рендера (важно)

Рендер MP4 выполняется Remotion-ом в headless-браузере. Какой браузер
использовать, задаёт переменная **`REMOTION_BROWSER_EXECUTABLE`** в `.env`:

- Укажите путь к установленному Chrome/Chromium — например,
  `/usr/bin/google-chrome` (Linux), `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` (macOS).
- Если переменная **не задана**, Remotion попытается **скачать из интернета**
  свой chrome-headless-shell; в окружении без сети рендер упадёт с ошибкой
  вида «could not download / no browser executable». Скачивание можно сделать
  заранее: `npx remotion browser ensure`.
- Всё остальное (сравнения, конструкторы, живое превью) работает **без**
  браузера — он нужен только кнопке «🎬 Рендерить видео».

## Роли

| Роль    | Возможности                                                                 |
|---------|------------------------------------------------------------------------------|
| Гость   | Только приветственный экран (описание возможностей + вход/регистрация)       |
| Пользователь | Топы и конструктор с живым превью; лимиты: 1 архив ≤100 МБ, пул медиа ≤100 МБ; рендер недоступен |
| Админ   | Всё без лимитов (архив до 10 ГБ) + админка: роли и удаление пользователей     |

Роль читается из БД на каждый запрос — смена роли действует сразу, без
перелогина. Проверки в коде идут по пермишенам через матрицу
`src/lib/domain/permissions.ts`; новая роль добавляется одной записью в неё.
**Личный кабинет** (`/account`): профиль, квоты «занято X из Y», менеджер
архивов/пула/рендеров с размерами — удаление стирает файлы с сервера.

## Поток использования

1. Регистрация / вход.
2. **Создать топ**: название, схема (сравнительная сортировка / швейцарка / круговая),
   **сколько сравнивать за раз** (2–7), слепой режим (опц.), ZIP-архив с треками —
   аудио (mp3/m4a/flac/…) и/или видео (mp4/webm/mov, рекомендуется MP4 H.264+AAC),
   лимит 10 ГБ (админ) / 100 МБ (пользователь). Инструкция «Как подготовить архив»
   — прямо на форме.
3. **Сравнения**: прослушать/просмотреть варианты и ответить. Пара — как раньше:
   лево / ничья / право. Группа из 3–7 — **клики по порядку**: первый клик даёт
   первое место, последний оставшийся встаёт сам, цифровые клавиши работают как
   клики. Не хочется различать хвост — «остальные примерно равны». Видео-треки
   играют в видеоплеере; в слепом режиме скрываются и названия, и видеоряд (трек
   играет как аудио до «Показать названия»). Прогресс сохраняется.
4. **Итоговый топ**: выбрать размер top-N.
5. **Конструктор рендера**: единый пул медиа (картинки + видео; массовая загрузка
   drag&drop, поиск, фильтр по типу, «недавние», постеры для видео). На каждую
   позицию — визуал (картинка, видео из пула или собственный видеоряд видео-трека)
   с недеструктивной обрезкой 16:9; звук — от трека или от вставленного видео
   (полная подмена OST видеорядом); сдвиг старта видеоряда, короткое видео
   зацикливается. Фрагмент — ручной таймлайн или активный сниппет; живой
   предпросмотр.
6. **Рендер** → скачать видео.

**Множественные сравнения (сколько за раз)** — отдельная настройка, а не ещё
одна схема: схема решает, кого с кем спрашивать, а размер группы — сколько
треков попадёт на один экран. Смысл в прослушиваниях, а не в кликах: одно
прослушивание в группе обслуживает несколько сравнений, поэтому ранжирование
тройки даёт 1,7× информации на прослушивание, пятёрки — 2,8×. Важная деталь:
платит именно **полное ранжирование** группы, а не «выбери лучшего из четырёх» —
второе по информации на прослушивание не лучше пары.

- Швейцарка и круговая работают с группами; **сравнительная сортировка — только
  парами** и честно об этом пишет: она и так работает на информационном пределе,
  и группы ей ничего не дают.
- Размер можно менять **прямо посреди турнира** — в базе всё равно лежат пары.
- План — «каждый трек получает `ceil(log₂ N)` соперников», формула не зависит от
  размера группы и при 2 сворачивается в нынешние `ceil(log₂ N)` раундов. На 201
  треке: пары — 800 экранов, тройки — 268, пятёрки — 82.
- План не тюрьма: рядом с прогрессом видно, **на сколько процентов пар порядок
  уже определён** (транзитивное замыкание лога), и когда план пройден, можно
  либо смотреть топ, либо взять **ещё раунд**. На реальных данных: тот же
  бюджет прослушиваний, что и сегодня, поднимает определённость с 36 % до 99 %.
- Индикатор честный: он же показывает **противоречия**. Живые предпочтения
  нетранзитивны (в реальном турнире 28 % треков оказались в циклах), и такие
  пары не «дорешиваются» в порядок, а показываются как противоречия.
- «Отменить последний ответ» убирает **экран целиком**, а не одну его пару.

**Источники в менеджере медиа** собраны в один ряд табов — «Файлы · Ссылка ·
Shikimori» — так сетка пула начинается сразу под активным источником, а не под
тремя развёрнутыми панелями. На любом табе работает **вставка из буфера**
(`Ctrl+V`): скриншот или скопированный файл загружается как обычный файл и
получает имя «Вставка ДД.ММ ЧЧ:ММ» (если буфер отдал безымянный `image.png`), а
картинка, скопированная в браузере, приезжает ссылкой и уходит в импорт по
ссылке. Вставка в текстовое поле остаётся обычной вставкой текста.

**Картинки по ссылке**: поле ссылки одно на все виды медиа. Если адрес ведёт на
картинку (`jpg`/`png`/`webp`/`gif`), она скачивается **сразу** (`POST
/api/arts/from-url`) и появляется в пуле — без фоновой джобы. Ограничения:
только `http(s)`, `Content-Type` должен быть картинкой, 32 МБ, таймаут 15 с, не
больше 3 редиректов, и ни один хоп не может вести в приватный диапазон адресов
(защита от SSRF; для домашней установки с медиа на NAS это снимается
`MEDIA_URL_ALLOW_PRIVATE=1` в `.env`). Если по ссылке оказалась не картинка,
рядом появляется кнопка «Скачать как видео/аудио» — тот же адрес уходит в
yt-dlp, перенабирать не нужно.

**Импорт видео и аудио по ссылке**: в менеджере медиа можно вставить ссылку на видеоролик
(YouTube и другие сайты, поддерживаемые yt-dlp) — сервер скачает его **фоново**
в пул: режимы «Видео до 480/720/1080p» (H.264+AAC mp4) и «Только звук» (m4a).
Прогресс и отмена — в том же окне; окно можно закрыть, файл появится в пуле.
Квота пула действует: оценка размера до старта + жёсткий кап во время
скачивания. Бинарник yt-dlp приложение само скачивает с официального GitHub
в `storage/bin` при первом использовании (переопределяется `YTDLP_PATH` в
`.env`) и самообновляется, когда YouTube ломает старые версии.

> **Если ссылка распознаётся (название видно), а загрузка падает с ошибкой
> про видео-CDN** — сеть сервера блокирует `googlevideo.com` (типичная
> ситуация: youtube.com открывается, CDN — нет). Решение: задать прокси/VPN
> в `YTDLP_PROXY` в `.env` (http:// или socks5://) и перезапустить сервер.

**Уборка пула**. У карточки видно, чем занят файл: «1 карточка в раунде, 1 фон»,
«3 позиции топа», «не используется» — считаются все ссылки на медиа (позиции
топов, карточки раундов пикера, треки плейлиста, фоны и музыка проектов/раундов),
а не только позиции. Подтверждение удаления называет последствия: карточки
раундов и треки плейлиста **удаляются вместе с медиа** (каскад), позиции топов
только освобождаются. Чекбокс в углу карточки включает выделение, `Shift`-клик
берёт диапазон, «Удалить выбранные» сносит до 200 файлов за раз и честно
сообщает, что не получилось. Имя правится на месте: карандаш подсказывает, что
поле редактируемое, `Enter` сохраняет с отметкой «✓ сохранено», `Esc` отменяет,
а отказ сервера показывается текстом и откатывает поле.

**Личный кабинет** (`/account`) показывает сводки, а списки грузит по запросу:
«Пул медиа: 1332 файла · 213 МБ · картинки 1312 · видео 0 · аудио 20» и кнопка
«Показать все» — дальше тот же список, что в менеджере (поиск, фильтр по типу,
миниатюры, переименование, выделение и удаление), страницами по 40. Так же
устроены «Архивы» и «Рендеры». Сводка считается агрегатами, размер готового MP4
берётся из `RenderJob.outputBytes` (пишется при завершении рендера), поэтому
страница больше не тянет тысячу строк и не делает `stat` на каждый файл: на
рабочем аккаунте это 447 Б ответа вместо 196 КБ.

**Поиск в Shikimori**: рядом с импортом по ссылке в менеджере медиа есть блок
поиска Shikimori — переключатель «Аниме / Персонажи», поле с дебаунсом ~300 мс
и живой дропдаун (мини-постер + факты: год · тип · рейтинг для аниме, имя
ru/en для персонажа). Клик «⬇ В пул» **синхронно** качает постер и добавляет
его в пул как обычную картинку (`Art`, с учётом квоты и роли). Базовый хост
задаётся `SHIKIMORI_BASE_URL` в `.env` (домен API периодически мигрирует —
смена одной переменной переносит интеграцию на новый хост); `SHIKIMORI_USER_AGENT`
задаёт заголовок User-Agent для запросов к API.

Скачивается **тот постер, который показан на сайте**, в полном разрешении:
старый REST-путь `/system/animes/original/<id>.jpg` отдаёт уменьшенную и иногда
устаревшую копию (например «Атака титанов»: 225×350 против 685×975 на сайте), а
актуальный файл берётся из GraphQL-поля `poster.originalUrl`
(`/uploads/poster/...`). Если свежий URL недоступен, импорт молча откатывается на
старый путь. Запросы к Shikimori соблюдают оба лимита сервиса (5 в секунду и 90 в
минуту): они выстраиваются в очередь со скользящим окном, а 429 повторяется с
паузой (с приоритетом заголовка `Retry-After`) — большие агентские сценарии на
сотни запросов больше не отваливаются по лимиту.

**Мои видео** (`/projects`) — проекты вне турнира: «Ручной топ» (тот же
конструктор, но позиции добавляются из пула — аудио или видео со звуком) и
«Пикер» (раунды: правило, 2–9 блоков с красивой сеткой — 5 это 3+2 по центру,
время показа, скрытие после показа, таймер с тик-звуком, фон-картинка/видео,
подписи и «правильный ответ»). Фоновая музыка пикера — **плейлист**: играет
непрерывно через все раунды, треки сменяются кроссфейдом ~1 с, по окончании
списка — луп; раунд со своей музыкой временно глушит плейлист. Подбор
фрагмента/старта — **на слух**: на позиции топа и видео-блоке пикера
раскрывается плеер исходника с кнопками «Старт отсюда» и «▶ Фрагмент»
(играет ровно выбранный кусок). Живое превью всего видео — в обоих
конструкторах.

**Интро и аутро пикера**: как и у топа, видео пикера может начинаться титульным
экраном и заканчиваться финальным (по 3 секунды каждый, тёмный фон + крупный
текст). Оба включаются галочками в «Настройках видео»; пустое поле текста берёт
название проекта (интро) и «Спасибо за просмотр» (аутро). Фоновая музыка играет
и на титрах. Новые проекты создаются с включёнными экранами, у ранее созданных
они выключены — чтобы уже собранные сценарии не поменяли длительность.

**Ориентация плиток пикера**: блоки раунда можно сделать **landscape** (16:9,
как раньше) или **portrait** (2:3, «вертикальные плашки» под формат
Shorts/Reels) — ориентация задаётся дефолтом на проекте и может быть
переопределена на конкретном раунде. Portrait-раскладка — отдельная сетка
«Ростер»: до 6 плиток в один ряд, 7–9 — два ряда (4+3 / 4+4 / 5+4), в отличие
от лендскейпа она не пытается сложить квадратный блок из пар рядов. Смена
эффективной ориентации сбрасывает недеструктивный кроп плитки (старый кроп
рассчитан под другие пропорции и был бы бессмысленным). У каждой плитки также
есть свой **режим вписывания** картинки/видео в рамку: `cover` (обрезка по
центру, по умолчанию), `fill` (растянуть на всю рамку, игнорируя пропорции)
и `contain` (вписать целиком, с полями/леттербоксом).

**Групповое сравнение (блок к блоку)**: у раунда есть режим — обычный (2–9
отдельных блоков, выбираем один) или **групповое сравнение**: 2–3 **блока** по
1–5 **карточек**, всего не больше 15. Так делаются раунды 3 на 3, 2 на 2 на 2 и
несимметричные вроде 3 против 2 или «один против трёх». В кадре блок — это
полупрозрачная плашка с названием (по умолчанию «Блок А/Б/В»), между плашками
знак **VS**. Размер карточки один на весь раунд: у блока с меньшим числом
карточек плашка просто уже — ни один персонаж не выглядит крупнее соседа. Блок
из 4–5 карточек сам переходит в два ряда, когда так карточка получается крупнее,
а блоки одного размера всегда выглядят одинаково. Шаг таймлайна — блок: за
«время показа блока» блок появляется целиком, карточки внутри выскакивают
каскадом по 0.15 с; `hideAfterReveal` гасит блок целиком. Правильный ответ
отмечается **на блоке** и не обязателен: плашка победителя светится золотом,
остальные гаснут. Если в блоке несколько видео со звуком — звучит первое, чтобы
не превращать раунд в шум. В конструкторе карточки переносятся между блоками
кнопками ⇦/⇨, порядок внутри блока — ←/→; групповой раунд попадает в видео, когда
в нём минимум два блока и в каждом есть карточка.

## MCP-сервер (ИИ сам собирает пикер)

Приложение умеет работать как **MCP-сервер** (stdio): внешний ИИ-клиент
(Claude Desktop / Claude Code) сам находит материал в Shikimori, импортирует
постеры и аудио в пул и раскладывает их по раундам проекта «Пикер». Рендер
остаётся в UI — инструменты возвращают ссылку `/projects/<id>`, вы проверяете
и рендерите проект сами.

### Запуск

MCP-сервер действует от имени одного аккаунта. Укажите его email и запустите:

```bash
# .env
MCP_ACTOR_EMAIL="you@example.com"   # email уже зарегистрированного аккаунта
# необязательно: папки, из которых ИИ может забирать медиа с диска (через ':')
MCP_LOCAL_MEDIA_DIRS="~/Музыка/ost:~/Картинки/фоны"
# необязательно: разрешить импорт картинок по ссылке с адресов локальной сети
# (по умолчанию приватные адреса запрещены — защита от SSRF)
MEDIA_URL_ALLOW_PRIVATE=1

npm run mcp
```

Без `MCP_LOCAL_MEDIA_DIRS` инструменты `list_local_media`/`import_local_media`
отвечают `LOCAL_MEDIA_DISABLED`: доступа к диску по умолчанию нет. Путь вне
разрешённых папок (в том числе через `..` или симлинк) — `PATH_NOT_ALLOWED`.
Оригиналы файлов не перемещаются: в пул уходит копия.

Права и квоты берутся из роли этого аккаунта (импорт требует роль с
`media:upload`; размер пула ограничен квотой роли).

### Подключение из Claude Desktop / Claude Code

В конфиге MCP-клиента (например `claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "tournament-bracket-video": {
      "command": "npm",
      "args": ["run", "-s", "mcp"],
      "cwd": "/абсолютный/путь/к/tournament-bracket-video",
      "env": { "MCP_ACTOR_EMAIL": "you@example.com" }
    }
  }
}
```

### Доступные инструменты

| Инструмент | Назначение | Возвращает |
|---|---|---|
| `shikimori_find_studio` | найти студию по названию | `[{id, name}]` |
| `shikimori_studio_animes` | аниме студии (по популярности) | `[{id, type, label, posterPath, facts}]` |
| `shikimori_anime_characters` | персонажи аниме (по умолчанию Main) | `[{id, type, label, posterPath}]` |
| `shikimori_search` | поиск аниме/персонажа по названию | `[{id, type, label, posterPath, facts}]` |
| `shikimori_character` | персонаж и все его аниме (старые первыми); `debutYear` — год первого появления, то есть «эпоха» самого персонажа | `{id, label, posterPath, debutYear, animes:[{id, label, kind, year}]}` |
| `shikimori_anime` | аниме с производственными студиями (спонсоры отброшены), годом, оценкой и жанрами — основа раундов «студия против студии» | `{id, label, kind, year, score, posterPath, studios:[{id, name}], genres, facts}` |
| `shikimori_find_user` | пользователь по нику (или числовому id) | `{id, nickname, url, avatarUrl}` |
| `shikimori_user_anime_list` | список аниме пользователя с **его** оценками и статусами; фильтры `status` (`planned`/`watching`/`rewatching`/`completed`/`on_hold`/`dropped`/`all`), `minScore`, порядок `score`/`updated`/`name`, `limit` ≤ 500 | `{user, total, countsByStatus, matched, items:[{id, label, kind, posterPath, userScore, status, episodes, rewatches, updatedAt}]}` |
| `shikimori_user_favourites` | избранное пользователя: аниме и персонажи (пути постеров готовы к импорту) | `{user, animes, characters}` |
| `list_pool` | что уже лежит в пуле (фильтры `kind`/`query`, `filePath` абсолютный) | `{arts:[{id, label, kind, durationSec, hasAudio, sizeBytes, filePath, createdAt}], nextCursor}` |
| `list_local_media` | медиа-файлы папки на диске (только из `MCP_LOCAL_MEDIA_DIRS`) | `{dir, roots, files:[{name, path, kind, sizeBytes}]}` |
| `import_local_media` | импорт локальных файлов в пул (оригиналы остаются на месте, ≤ 50 за вызов) | `{items:[{artId, label, kind, durationSec, sizeBytes}], failed:[{path, error}]}` |
| `import_shikimori_poster` | импорт постера в пул как картинку | `{artId}` |
| `import_youtube_audio` | скачать звук с YouTube (yt-dlp) в пул | `{artId}` |
| `create_picker_project` | создать пикер (уже с 1 пустым раундом); опционально `orientation: "landscape"|"portrait"` — дефолт ориентации плиток проекта, `introText`/`outroText` — тексты титульного и финального экранов (пустая строка выключает экран) | `{projectId, firstRoundId}` |
| `add_round` | добавить раунд (вопрос/таймер/подписи); опционально `orientation` — переопределяет ориентацию только этого раунда, `mode: "groups"` — сразу групповое сравнение (раунд получает два пустых блока) | `{roundId, groupIds}` |
| `set_round` | изменить существующий раунд (промпт/таймер/подписи/ориентацию/`mode`); пустой `prompt` убирает вопрос; переключение `mode` раскладывает плитки по блокам и обратно | `{ok, groupIds}` |
| `add_group` | добавить блок в групповой раунд (≤ 3), опционально с названием | `{groupId}` |
| `set_group` | название блока и `isAnswer` — блок-победитель (один на раунд, необязателен) | `{ok}` |
| `delete_group` | удалить блок вместе с карточками | `{ok}` |
| `delete_round` | удалить раунд (например пустой стартовый — для чистого сценария) | `{ok}` |
| `add_tile` | плитка из готового арта: `roundId` для обычного раунда или `groupId` для блока; опционально `fitMode: "cover"|"fill"|"contain"` | `{tileId}` |
| `add_tile_from_shikimori` | импорт постера + карточка одним вызовом (`roundId` или `groupId`); опционально `fitMode` | `{tileId, artId}` |
| `set_playlist` | фоновая музыка пикера | `{ok}` |
| `set_project` | сам проект: задний фон (`backgroundArtId`, `null` снимает), название, ориентация, тексты интро/аутро, `revealSec`/`timerSec`/`hideAfterReveal`/`tickSound` | `{ok}` |
| `get_project` | структура проекта для самопроверки (тексты `intro`/`outro`, `background`, `playlistSec`, плановый `durationSec`, `mode` раундов и блоки) | `summary` |

### Пример сценария (студия Madhouse, 4 варианта, 10 раундов)

Попросите ИИ: «Собери пикер персонажей студии Madhouse, по 4 варианта в
раунде, 10 раундов, в каждом отметь одного как правильный ответ». ИИ сам:
`shikimori_find_studio("Madhouse")` → `shikimori_studio_animes` →
`shikimori_anime_characters` → `create_picker_project` → циклом `add_round` +
`add_tile_from_shikimori` (один `isAnswer: true`) → `get_project` для сверки →
отдаёт ссылку на проект. Дальше вы открываете `/projects/<id>` и рендерите.

### Пример сценария «по моему списку на Shikimori»

Попросите ИИ: «Собери пикер по аниме, которые я смотрел — ник `andrey03410`,
только с оценкой 9 и выше». ИИ вызывает
`shikimori_user_anime_list({user:"andrey03410", status:"completed", minScore:9})`,
отбрасывает записи с `kind` `music`/`pv`/`cm` (у них нет персонажей), берёт
персонажей каждого аниме через `shikimori_anime_characters` и раскладывает их по
раундам. `shikimori_user_favourites` даёт избранных персонажей и аниме того же
пользователя — их постеры импортируются напрямую. Сводка `countsByStatus`
показывает, сколько у человека просмотрено/запланировано/отложено/брошено.

### Пример сценария «сравни тройки»

Попросите ИИ: «Собери раунд: тройка персонажей Clannad против тройки AIR, ответ —
Clannad». ИИ вызывает `add_round({projectId, mode:"groups", prompt:"Какая тройка
сильнее?"})`, получает `groupIds`, называет блоки через `set_group`, наполняет их
`add_tile_from_shikimori({groupId, ...})` и отмечает победителя
`set_group({groupId, isAnswer:true})`. `get_project` показывает состав блоков.

### Пример сценария «оформи пикер»

Попросите ИИ: «Поставь фоном картинку с зонтиком из пула и подбери спокойный
плейлист под хронометраж». ИИ вызывает `get_project` (там плановый
`durationSec`), `list_pool({kind:"image"})` для фона →
`set_project({backgroundArtId})`, затем `list_local_media({dir})` →
`import_local_media({paths})` → `set_playlist({artIds})` и сверяет
`playlistSec` с `durationSec`.

## Тесты

```bash
npm test
```

Покрытие (666 тестов):
- доменные движки турнира (merge / swiss / round-robin): завершаемость,
  возобновляемость, корректность ранжирования, тай-брейки, граничные случаи;
- множественные сравнения (фаза 18): разбор ответа группы в пары (полное
  ранжирование, ранний стоп, пара), целевое число соперников и оценка экранов;
  определённость порядка через транзитивное замыкание (цепочка, цикл, ничья не
  задаёт порядок, поле в 300 треков); движки с группами 2–5 — завершаемость, ни
  одной повторной пары, ноль противоречий на согласованных ответах, прогресс не
  выходит за 100 %, устойчивость к смене размера посреди раунда, к «всё примерно
  равно» на каждом экране и к покупке лишних раундов до исчерпания пар;
  merge держится пар при любом запросе;
- интеграционные тесты групповых сравнений на реальной БД: пачка на экран,
  ничьи для нерасставленного хвоста, отказ от устаревшего экрана, отмена экрана
  целиком (и повторный показ отменённого), кламп размера по полю и по схеме,
  «ещё раунд» поднимает определённость и упирается в исчерпание пар, номер
  экрана не обгоняет оценку, противоречия показываются, а не заметаются,
  завершённый турнир не открывается заново и остаётся доступен для смены top-N;
- RMS-выбор активного сниппета;
- расчёт плана видео (порядок, тайминги плашек/визуала, длительности);
- сборка плана рендера из конфига, кламп сегмента по длине источника звука;
- правила позиции «визуал × звук» (матрица картинка/видео/видеоряд-трека ×
  трек/видео), луп короткого видео, валидация сдвига старта;
- разбор имён/тегов при загрузке, определение типа файла (аудио/видео);
- валидация обрезки и отображение рамки в CSS (общий код превью и рендера);
- интеграционный тест полного потока на реальной БД (Prisma + движок + ранжирование
  + конфиг рендера);
- интеграционные тесты пула медиа на реальной БД (поиск/пагинация/недавние/фильтр
  по типу, загрузка видео с пробой длительности/звука и постером, переименование,
  удаление с освобождением позиций, назначение и обрезка, подмена звука,
  изоляция чужих данных);
- интеграционные тесты видео-сценариев: смешанный ZIP, беззвучное видео,
  превью-план (синхронный видеоряд, луп, оверлей картинкой);
- роли и пермишены: матрица прав/квот, лимит архивов (включая гонку двух
  параллельных загрузок), квота пула, бутстрап админа по `ADMIN_EMAILS`,
  сводка кабинета с бэкфиллом размеров, админ-операции (смена роли,
  удаление пользователя с файлами, защита от self-demote/self-delete);
- библиотека медиа (фаза 17): классификация ссылок (картинка / для yt-dlp /
  не ссылка), `Content-Type` → расширение, политика адресов (localhost,
  `*.local`, приватные и служебные диапазоны IPv4/IPv6, IPv4-mapped), метка из
  URL; чтение буфера (HTML `<img src>` важнее текста, текстовая ссылка только
  если это картинка, метка вставки, безымянные файлы); формулировки
  использований и их сумма по выделению, русские множественные формы; импорт
  картинки по ссылке против локального HTTP-сервера (успех, не-картинка,
  превышение лимита по заголовку и по факту байт, редирект на loopback, дохлая
  ссылка, квота — без осиротевших файлов в `storage/tmp`); честный счётчик
  использований по всем восьми связям `Art` и каскад при удалении; массовое
  удаление (частичные отказы, дубликаты, пустая и переполненная пачка);
  кабинет на агрегатах (разбивка пула по типам, бэкфилл размеров треков и
  `outputBytes`, страницы рендеров с курсором, изоляция чужих данных);
- групповое сравнение: геометрия блоков (симметрия и асимметрия 3v2 / 1v3,
  один размер карточки, выбор разбиения, одинаковый вид блоков одного размера,
  границы кадра), таймлайн (окна блоков, каскад карточек, ответ на блоке, звук
  первой карточки блока, пропуск недособранного раунда), сервис (переключение
  режима с переносом плиток, лимиты 3/5/15, перенос карточки, удаление блока,
  валидация раунда) и MCP-сценарий 3v2;
- пикер: раскладки 2–9 для обеих ориентаций плиток — landscape и portrait
  (центрирование неполных рядов, непересечение), fit-режимы cover/fill/contain
  (CSS обрезки/растяжения/вписывания), план (тайминги раундов, наследование
  дефолтов и ориентации/fitMode, дакинг-окна, таймер, фаза ответа, режимы
  подписей, сброс кропа при смене ориентации), плейлист (нарезка cue с
  кроссфейдом, луп, mute-окна раундов со своей музыкой), интро/аутро (сдвиг
  таймлайна, длительность, каждый экран по отдельности, пустой проект,
  непрерывность музыки на титрах);
- видео-проекты на реальной БД: CRUD раундов/плиток с валидациями (максимум
  9 блоков, один «ответ», сдвиг в пределах видео), аудио в пуле, ручной топ
  (источник звука позиции, кламп фрагмента, порядок), каскады удаления;
- импорт по ссылке: yt-dlp-аргументы/селекторы/парсер прогресса/классификатор
  ошибок (юнит) и полный конвейер на локальном HTTP-источнике (видео и аудио
  в пул с пробой/постером, мёртвая ссылка, блокировка по квоте, лимит
  2 параллельных, отмена, изоляция);
- импорт из Shikimori: маппинг аниме/персонажа, фолбэк подписи (русское имя →
  оригинал), валидатор пути постера от SSRF (чужой хост, не-`/system`,
  traversal, абсолютный URL) — юнит; и интеграционный конвейер на реальной БД
  с локальным HTTP-источником вместо Shikimori (поиск нормализуется,
  синхронный импорт постера создаёт `Art(image)` с учётом квоты, отказ по
  квоте, отказ импорта при невалидном пути);
- список и избранное пользователя Shikimori: маппинг профиля и записи списка
  (оценка 0 → «нет оценки», манга и неизвестные статусы отбрасываются), фильтры
  по статусу/оценке, три порядка, счётчики по статусам, избранное (миниатюра
  `x64` → путь постера, мусор пропущен), плейсхолдер «нет арта» → null — юнит;
  интеграционно на локальном стенде: ник и id, `USER_NOT_FOUND`, «список одним
  запросом», импорт постера избранного персонажа в пул, ретрай при 429 и явная
  ошибка после исчерпания попыток;
- лимиты и свежие постеры Shikimori: политика ожиданий (пауза, скользящее окно,
  устаревшие метки, расписание повторов и `Retry-After`) — юнит; гард на URL
  постера (чужой хост, другой протокол/порт, не-poster путь, traversal);
  интеграционно — импорт берёт свежие байты вместо legacy-копии, готовый
  `posterUrl` не тратит лишний GraphQL-запрос, три сценария фолбэка (нет
  постера / GraphQL 500 / чужой хост) дают legacy-копию.

Кроме того, вручную провалидированы (см. историю разработки):
- ffmpeg-пайплайн (декод/нарезка аудио и видео, постеры, тишина-фолбэк);
- полный HTTP E2E: логин → смешанный ZIP → сравнения → финал → конфиг рендера
  + превью + подмена/оверлей + негативные пробы;
- headless-рендер MP4 с видео-сегментами (кадры выходного файла проверены);
- ролевой E2E: гость/пользователь/админ, лимиты 403/413, смена роли без
  перелогина, кабинет и админка (UI-прогон в headless Chrome);
- E2E импорта по ссылке: полный конвейер по HTTP + UI-прогон (ссылка →
  прогресс → карточка в пуле), реальный YouTube (проба метаданных; полная
  загрузка зависит от доступности googlevideo.com из сети сервера);
- E2E импорта из Shikimori: UI-прогон в headless Chrome против реального
  shikimori.io — поиск «naruto» в режимах «Аниме» и «Персонажи» даёт живой
  дропдаун с постерами и фактами, клик «⬇ В пул» синхронно добавляет постер
  в пул (карточка появляется в сетке медиа-менеджера);
- E2E видео-проектов: жизненный цикл пикера с проверкой превью-плана (включая
  геометрию раскладки 3+2), негативные пробы, ручной топ, headless-рендер
  пикера и ручного топа с проверкой кадров MP4 (правило, плитки, таймер,
  подсветка ответа);
- headless-рендер пикера с интро/аутро: кадры титульного и финального экранов
  из MP4 + сходимость длительности файла с `previewPlan.durationSec`;
- MCP-инструменты списка/избранного на **реальном** shikimori.io (профиль
  `andrey03410`): прогон по stdio-протоколу — сводка по статусам совпадает с
  профилем, избранные персонажи приходят с рабочими путями постеров, импорт
  постера в пул проходит, несуществующий ник даёт `USER_NOT_FOUND`.

## Структура

```
src/
  lib/domain/        # чистая доменная логика (движки, ранжирование, RMS, план видео) + тесты
  lib/               # upload (zip/id3), audio (ffmpeg), storage, render-assemble, db, api
  server/            # сервисы (tournaments, render) — серверные, используют Prisma
  remotion/          # Remotion-композиция (TopVideo) — рендер и предпросмотр
  app/               # Next.js App Router: страницы + API-роуты
  integration/       # интеграционный тест на БД
prisma/schema.prisma # модель данных (обе фазы)
scripts/             # вспомогательные скрипты (e2e, очистка тестовых данных)
```

## Рендер видео

Рендер работает end-to-end (проверено: на выходе валидный MP4 H.264 1920×1080 @30fps).
Используется headless-режим `chrome-for-testing` с системным Chrome — путь задаётся
через `REMOTION_BROWSER_EXECUTABLE` (в `.env.example` указан `/usr/bin/google-chrome`).
Альтернатива — `npx remotion browser ensure` (скачает chrome-headless-shell). Детали:
[`.ai/research/remotion-headless-render.md`](.ai/research/remotion-headless-render.md).
